apiVersion: v1
kind: Service
metadata:
  name: karma
  namespace: monitoring
  labels:
    app: karma
spec:
  selector:
    app: karma
  ports:
    - name: http
      port: 8080
      targetPort: http
